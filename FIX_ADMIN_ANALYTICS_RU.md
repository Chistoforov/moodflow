# Исправление отображения аналитики в админке

## 🔍 Проблема
В админ-панели при просмотре пользователя не отображаются рекомендации и аналитика, хотя у самого пользователя эти данные есть.

## 🎯 Причина
Политики безопасности Row Level Security (RLS) в базе данных не позволяют админам читать данные аналитики других пользователей. Возможно, миграция с исправлением не была применена в продакшене.

## ✅ Решение
Применить миграцию `025_fix_admin_analytics_access.sql`, которая создает политики для доступа админов ко всей аналитике.

## 🚀 Быстрое применение

### Способ 1: Через скрипт (рекомендуется)

```bash
./apply_migration_025.sh
```

### Способ 2: Через Supabase CLI

```bash
npx supabase db push
```

### Способ 3: Через Supabase Dashboard (если CLI не работает)

1. Откройте https://app.supabase.com
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Создайте новый запрос
5. Откройте файл `supabase/migrations/025_fix_admin_analytics_access.sql`
6. Скопируйте весь его текст
7. Вставьте в SQL Editor
8. Нажмите **Run** (или `Cmd/Ctrl + Enter`)

## 📝 Что делает миграция

Миграция создает следующие политики для таблицы `monthly_analytics`:

1. **Для обычных пользователей:**
   - Чтение своей аналитики
   - Создание своей аналитики
   - Обновление своей аналитики

2. **Для админов:**
   - Чтение аналитики всех пользователей ✨
   - Обновление аналитики всех пользователей ✨
   - Создание аналитики для любого пользователя ✨

3. **Для сервисной роли:**
   - Полный доступ ко всем операциям

## ✓ Проверка результата

После применения миграции:

1. Перезагрузите страницу админ-панели (Cmd/Ctrl + R)
2. Откройте страницу пользователя, у которого есть аналитика
3. Убедитесь, что секция "Аналитика и рекомендации" показывает данные

## 🔍 Диагностика проблем

### Если аналитика все еще не видна:

#### 1. Проверьте консоль браузера
- Нажмите F12
- Перейдите на вкладку Console
- Обновите страницу
- Проверьте наличие ошибок (красный текст)

#### 2. Проверьте Network запросы
- Нажмите F12
- Перейдите на вкладку Network
- Обновите страницу
- Найдите запрос `/api/admin/users/[userId]/analytics`
- Проверьте статус (должен быть 200)
- Проверьте ответ (Response)

#### 3. Проверьте статус админа в БД

Выполните в SQL Editor:

```sql
-- Проверить вашу роль
SELECT * FROM psychologists WHERE user_id = auth.uid()::text;
```

Должно быть:
- `role = 'admin'`
- `active = true`

Если нет, исправьте:

```sql
UPDATE psychologists 
SET role = 'admin', active = true 
WHERE email = 'ваш-email@example.com';
```

#### 4. Проверьте наличие данных

```sql
-- Замените USER_ID на ID пользователя, которого смотрите
SELECT * FROM monthly_analytics 
WHERE user_id = 'USER_ID' 
ORDER BY created_at DESC;
```

Если данных нет, попробуйте сгенерировать аналитику через кнопку "Получить рекомендации" в админ-панели.

## 📚 Дополнительные файлы

- `supabase/migrations/025_fix_admin_analytics_access.sql` - сама миграция
- `APPLY_MIGRATION_025.md` - подробная инструкция на английском
- `CHECK_ANALYTICS_POLICIES.sql` - скрипт для проверки политик
- `apply_migration_025.sh` - скрипт для автоматического применения

## ⚠️ Важно

- Миграция безопасна и может применяться многократно
- Существующие данные не будут изменены
- Изменяются только правила доступа (RLS policies)
- После применения миграции изменения вступают в силу немедленно

## 💡 Если ничего не помогает

1. Проверьте логи Vercel (если приложение деплоится там)
2. Проверьте переменные окружения (SUPABASE_URL и SUPABASE_ANON_KEY)
3. Убедитесь, что вы авторизованы как админ
4. Попробуйте выйти и зайти снова в админ-панель

---

**Время применения:** ~1-2 минуты  
**Риск:** Минимальный (только изменение политик доступа)  
**Откат:** Не требуется (политики можно пересоздать в любой момент)
