# Срочное исправление ошибки 404 при анализе

## Проблема
API endpoint `/api/admin/analytics/manual` возвращает 404

## Что было сделано
1. ✅ Код API существует и корректен
2. ✅ Создан пустой коммит для триггера редеплоя
3. ✅ Push на GitHub выполнен успешно
4. ⏳ Vercel сейчас выполняет автоматический деплой (1-2 минуты)

## Следующие шаги

### Шаг 1: Дождитесь завершения деплоя на Vercel

1. Откройте: https://vercel.com/dashboard
2. Найдите проект `moodflow-six`
3. Убедитесь, что последний деплой завершился успешно (зеленая галочка)
4. Время деплоя: ~1-2 минуты с момента push (22:51)

### Шаг 2: Проверьте RLS политики в Supabase

После завершения деплоя нужно убедиться, что в Supabase применены правильные RLS политики.

#### 2.1. Проверить существующие политики

Откройте Supabase Dashboard → SQL Editor и выполните:

```sql
-- Проверить какие политики существуют
SELECT 
  schemaname,
  tablename,
  policyname,
  permissive,
  roles,
  cmd,
  qual,
  with_check
FROM pg_policies 
WHERE tablename = 'monthly_analytics'
ORDER BY policyname;
```

#### 2.2. Проверить какие миграции применены

```sql
SELECT version, name, executed_at
FROM supabase_migrations.schema_migrations 
WHERE name LIKE '%monthly_analytics%' 
ORDER BY version DESC;
```

### Шаг 3: Применить недостающие миграции

Если миграция `025_fix_monthly_analytics_admin_insert_fixed.sql` НЕ применена, выполните в SQL Editor:

```sql
-- Fix: Allow admins to insert monthly analytics for any user
-- ВАЖНО: Эта миграция добавляет политики для админов

-- Удалить старые политики если есть (чтобы избежать конфликтов)
DROP POLICY IF EXISTS "Admins can insert monthly analytics for any user" ON public.monthly_analytics;
DROP POLICY IF EXISTS "Admins can update monthly analytics" ON public.monthly_analytics;
DROP POLICY IF EXISTS "Admins can delete monthly analytics" ON public.monthly_analytics;

-- Добавить новые политики
CREATE POLICY "Admins can insert monthly analytics for any user"
  ON public.monthly_analytics
  FOR INSERT
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM public.psychologists p
      JOIN public.users u ON p.user_id = u.sso_uid
      WHERE u.id = auth.uid()
      AND p.role = 'admin'
      AND p.active = true
    )
  );

CREATE POLICY "Admins can update monthly analytics"
  ON public.monthly_analytics
  FOR UPDATE
  USING (
    EXISTS (
      SELECT 1 FROM public.psychologists p
      JOIN public.users u ON p.user_id = u.sso_uid
      WHERE u.id = auth.uid()
      AND p.role = 'admin'
      AND p.active = true
    )
  );

CREATE POLICY "Admins can delete monthly analytics"
  ON public.monthly_analytics
  FOR DELETE
  USING (
    EXISTS (
      SELECT 1 FROM public.psychologists p
      JOIN public.users u ON p.user_id = u.sso_uid
      WHERE u.id = auth.uid()
      AND p.role = 'admin'
      AND p.active = true
    )
  );
```

### Шаг 4: Проверить работу API

После применения миграций:

1. Откройте админ панель: https://moodflow-six.vercel.app/admin/users
2. Выберите пользователя
3. Нажмите "Запустить анализ"
4. Проверьте логи в Network tab

Ожидаемый результат:
- ✅ Статус 200 (не 404)
- ✅ В ответе должно быть: `{ message: 'Analysis completed successfully', ... }`

### Шаг 5: Если 404 остается

Если после деплоя всё еще 404:

1. **Hard refresh** страницы: `Cmd + Shift + R` (Mac) или `Ctrl + Shift + R` (Windows)
2. **Очистить кэш браузера** для moodflow-six.vercel.app
3. **Проверить логи деплоя** в Vercel:
   - Откройте Deployments
   - Кликните на последний деплой
   - Проверьте Build Logs
   - Убедитесь что build завершился без ошибок

## Время выполнения

- ⏳ Деплой на Vercel: ~1-2 минуты (до 22:53)
- ⚡ Применение миграций: ~10 секунд
- ✅ Проверка работы: ~30 секунд

**Итого: ~3 минуты с текущего момента**

## Проверка завершения

✅ Деплой завершен когда:
- В Vercel Dashboard статус деплоя "Ready"
- Timestamp последнего деплоя = сейчас (22:51-22:53)

✅ RLS политики применены когда:
- В результате SQL запроса видны 3 политики для админов:
  - "Admins can insert monthly analytics for any user"
  - "Admins can update monthly analytics"
  - "Admins can delete monthly analytics"

✅ Всё работает когда:
- API возвращает 200 вместо 404
- В логах видно "Analysis completed successfully"


