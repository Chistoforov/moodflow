# 🎉 Админ-панель готова!

## Что было сделано

### ✅ 1. База данных
- Создана миграция `019_add_initial_admin.sql`
- Email `site4people@gmail.com` будет автоматически получать права админа при первом входе
- Создан триггер для автоматического добавления админа

### ✅ 2. Роуты админ-панели

Админка доступна по адресу **`/admin`**

#### Три раздела:

**1. Dashboard** (`/admin/dashboard`)
- Главная страница с статистикой

**2. Пользователи** (`/admin/users`)
- Список всех пользователей
- Email, имя, дата регистрации
- **Выпадающий список для смены роли:**
  - Бесплатный
  - Подписка
  - Личный психолог
  - Админ

**3. Материалы** (`/admin/materials`)
- Список всех материалов (опубликованные + черновики)
- Статус публикации
- Дата публикации
- Кнопки редактирования и удаления
- **Зелёная кнопка "+"** для создания нового материала

### ✅ 3. Создание/редактирование материалов

При нажатии на "+" или "редактировать" открывается **большая модалка** с:

1. **Поле заголовка** - обычное текстовое поле
2. **WYSIWYG редактор** для текста с форматированием:
   - Заголовки (H1, H2, H3)
   - Жирный, курсив, подчёркивание, зачёркивание
   - Маркированные и нумерованные списки
   - Ссылки
   - Очистка форматирования
3. **Дата публикации** (опционально):
   - Если не заполнена → публикуется сразу
   - Если указана будущая дата → материал будет в черновиках до этой даты

### ✅ 4. Защита доступа

- Только админы могут зайти в `/admin`
- Вход через Google SSO
- Middleware проверяет права на уровне роутинга
- API эндпоинты проверяют права на уровне сервера

## 🚀 Что нужно сделать сейчас

### Шаг 1: Выполнить миграцию

Откройте **Supabase Dashboard** → **SQL Editor** и выполните файл:
```
supabase/migrations/019_add_initial_admin.sql
```

### Шаг 2: Задеплоить

```bash
npm install
npm run build
```

Затем задеплойте на Vercel или ваш хостинг.

### Шаг 3: Войти

1. Откройте ваш сайт
2. Войдите через Google как `site4people@gmail.com`
3. Перейдите на `/admin`

## 📋 API эндпоинты (созданы автоматически)

### Пользователи
- `GET /api/admin/users` - список всех пользователей
- `PATCH /api/admin/users/[userId]` - изменить роль

### Материалы
- `GET /api/admin/posts` - список всех материалов
- `POST /api/admin/posts` - создать материал
- `GET /api/admin/posts/[postId]` - получить один материал
- `PATCH /api/admin/posts/[postId]` - обновить материал
- `DELETE /api/admin/posts/[postId]` - удалить материал

## 🎨 Дизайн

- Следует дизайн-системе приложения (бордовый + бежевый)
- Адаптирован под работу на ПК (не мобильный)
- Современный UI с плавными анимациями

## 🔑 Как добавить ещё админов

1. Войдите в админку
2. Откройте **Пользователи**
3. Найдите нужного пользователя
4. В выпадающем списке выберите **"Админ"**
5. Готово! Теперь этот пользователь может заходить в `/admin`

## 📝 Важные моменты

1. **Первый вход обязателен**: `site4people@gmail.com` должен войти через Google SSO хотя бы один раз, чтобы триггер сработал

2. **Роль Админ** отличается от подписок:
   - Бесплатный/Подписка/Личный психолог - это уровни доступа к функциям приложения
   - Админ - это доступ к панели управления (добавляется в отдельную таблицу `psychologists`)

3. **Slug автоматический**: URL материалов генерируются автоматически из заголовка

4. **React Quill**: Установлен с `--legacy-peer-deps` для совместимости с React 19

## 🎯 Результат

Теперь у вас есть полноценная админ-панель для:
- ✅ Управления пользователями и их ролями
- ✅ Создания и редактирования материалов с WYSIWYG редактором
- ✅ Планирования публикаций на будущее
- ✅ Удаления материалов

Всё работает через Google SSO, только админы имеют доступ, дизайн выдержан в стиле приложения!

