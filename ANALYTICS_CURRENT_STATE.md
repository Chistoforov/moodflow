# Текущее состояние аналитики и рекомендаций

## ✅ Что уже реализовано

### 1. Сбор данных
- Пользователь заполняет настроение ежедневно в таблице `daily_entries`
- Записывается: `mood_score` (1-5), `text_entry`, `factors`, `transcript`

### 2. Автоматическая генерация аналитики (Крон)
**Файл:** `src/app/api/cron/monthly-analytics/route.ts`
**Расписание:** Ежедневно в 22:00 UTC (настроено в `vercel.json`)

**Логика:**
- Проверяет каждые 7 дней или последний день месяца
- Отправляет накопительные данные:
  - День 7: отправляет за 7 дней
  - День 14: отправляет за 14 дней
  - День 21: отправляет за 21 день
  - Последний день месяца: отправляет весь месяц
- Работает только для пользователей с активной подпиской (`subscription` или `personal`)

### 3. Интеграция с Perplexity
**Файл:** `src/lib/integrations/perplexity.ts`
**Модель:** `sonar-pro`

**Промпт структурирован по разделам:**
1. Общее впечатление о периоде
2. Положительные тенденции
3. Возможные причины спада
4. Рекомендации и техники
5. Направление для размышлений

Результат сохраняется в таблицу `monthly_analytics`.

### 4. API для пользователя
**GET** `/api/analytics?year=2024&month=12`
- Получает аналитику текущего пользователя
- Возвращает последнюю запись (приоритет финальной аналитике)

**POST** `/api/analytics/generate`
- Генерирует аналитику вручную
- Проверяет, не существует ли уже аналитика за эту неделю

### 5. API для админа
**GET** `/api/admin/users/[userId]/analytics?year=2024&month=12`
- Получает аналитику пользователя

**POST** `/api/admin/users/[userId]/analytics/generate`
- Генерирует/обновляет аналитику для пользователя
- Отправляет все данные месяца в Perplexity

**PATCH** `/api/admin/users/[userId]/analytics`
- Обновляет аналитику (редактирование админом)

### 6. UI для пользователя
**Страница:** `/calendar`
- Отображает календарь с настроением
- Показывает секцию "Аналитика и рекомендации"
- Все 5 разделов аналитики отображаются красиво

### 7. UI для админки
**Страница:** `/admin/users/[userId]`
- Таблица с записями пользователя за месяц
- Панель "Аналитика и рекомендации" справа
- Кнопка "Получить рекомендации" / "Обновить рекомендации"
- Возможность редактировать аналитику (кнопка карандаша)

---

## ❌ Выявленные проблемы

### Проблема 1: Аналитика не отображается в админке

**Симптом:** У пользователя на странице календаря аналитика есть, но в админке не отображается.

**Причины:**

1. **Миграция 025 не применена на сервере**
   - Файл `supabase/migrations/025_fix_admin_analytics_access.sql` в untracked состоянии
   - RLS политики для админа могут не работать

2. **API админки использует неправильную логику**
   ```typescript
   // Текущий код в /api/admin/users/[userId]/analytics/route.ts
   .order('created_at', { ascending: false })
   .limit(1)
   .single()  // ❌ Вызовет ошибку если записи нет
   ```
   
   **Должно быть:**
   ```typescript
   .order('is_final', { ascending: false })
   .order('week_number', { ascending: false })
   .limit(1)
   .maybeSingle()  // ✅ Вернет null если записи нет
   ```

### Проблема 2: Крон работает только для платных пользователей

Текущий код:
```typescript
.in('subscription_tier', ['subscription', 'personal'])
.eq('subscription_status', 'active')
```

**Вопрос:** Нужно ли генерировать аналитику для бесплатных пользователей?

---

## 📋 План исправлений

### Шаг 1: Применить миграцию 025
```bash
# Добавить миграцию в git
git add supabase/migrations/025_fix_admin_analytics_access.sql

# Применить на сервере через Supabase Dashboard:
# Supabase -> SQL Editor -> Вставить содержимое 025_fix_admin_analytics_access.sql
```

### Шаг 2: Исправить API админки
Файл: `src/app/api/admin/users/[userId]/analytics/route.ts`

Изменить логику получения данных:
- Заменить `.single()` на `.maybeSingle()`
- Изменить сортировку на приоритет финальной аналитике

### Шаг 3: Проверить работу
1. Проверить в админке, отображается ли аналитика
2. Протестировать кнопку "Получить рекомендации"
3. Протестировать редактирование аналитики

---

## 🔧 Рекомендации

### 1. Логирование
Добавить логирование в API админки для отладки:
```typescript
console.log('Fetching analytics for user:', userId, 'year:', year, 'month:', month)
console.log('Analytics result:', analytics)
```

### 2. Обработка ошибок
Улучшить сообщения об ошибках для админа:
```typescript
if (error && error.code !== 'PGRST116') {
  console.error('Error details:', error)
  return NextResponse.json({ 
    error: 'Failed to fetch analytics', 
    details: error.message 
  }, { status: 500 })
}
```

### 3. Мониторинг крон-джоба
Проверить логи Vercel:
- Vercel Dashboard -> Cron Jobs -> Logs
- Убедиться, что крон запускается успешно
- Проверить, генерируется ли аналитика для пользователей

---

## 📊 Текущая структура таблицы monthly_analytics

```sql
CREATE TABLE monthly_analytics (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  year INTEGER NOT NULL,
  month INTEGER NOT NULL,
  week_number INTEGER NOT NULL,
  days_analyzed INTEGER NOT NULL,
  analysis_text TEXT NOT NULL,
  general_impression TEXT,
  positive_trends TEXT,
  decline_reasons TEXT,
  recommendations TEXT,
  reflection_directions TEXT,
  is_final BOOLEAN DEFAULT false,
  status TEXT DEFAULT 'completed',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(user_id, year, month, week_number)
)
```

**RLS Политики (из миграции 025):**
- Users can read their own analytics
- Service role can manage all analytics
- Users can insert/update their own analytics
- **Admins can read/update/insert all analytics** ← Важно для админки

---

## 🎯 Следующие шаги

1. ✅ Применить миграцию 025
2. ✅ Исправить API админки
3. ✅ Протестировать в админке
4. ✅ Проверить крон-джоб в логах Vercel

После исправлений механика будет работать так:
- Крон автоматически генерирует аналитику каждые 7 дней
- Пользователь видит аналитику на странице календаря
- Админ видит аналитику в админке и может:
  - Просмотреть существующую аналитику
  - Сгенерировать/обновить аналитику вручную
  - Отредактировать любое поле аналитики
