# 🚀 Деплой MoodFlow на Vercel

## 📋 Предварительные требования

1. Аккаунт на [Vercel](https://vercel.com)
2. Проект настроен и работает локально
3. GitHub репозиторий (рекомендуется) или готовность к деплою через Vercel CLI

## 🔧 Способ 1: Деплой через Vercel Dashboard (Рекомендуется)

### Шаг 1: Подготовка проекта

Убедитесь, что проект готов:

```bash
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow
npm run build
```

Если билд проходит успешно, можно деплоить.

### Шаг 2: Импорт проекта на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New Project"**
3. Импортируйте ваш репозиторий (GitHub/GitLab/Bitbucket) или загрузите код
4. Выберите папку `moodflow` как **Root Directory** (если проект в монорепозитории)

### Шаг 3: Настройка переменных окружения

В настройках проекта Vercel перейдите в **Settings → Environment Variables** и добавьте:

#### Обязательные переменные:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
NEXT_PUBLIC_APP_URL=https://moodflow-six.vercel.app
```

#### Опциональные (если используются):

```bash
PERPLEXITY_API_KEY=pplx-...
TELEGRAM_BOT_TOKEN=1234567890:ABC...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_PRICE_SUBSCRIPTION=price_...
STRIPE_PRICE_PERSONAL=price_...
CRON_SECRET=ваш_секретный_ключ
```

**Важно**: 
- Выберите все окружения (Production, Preview, Development) для `NEXT_PUBLIC_*` переменных
- `SUPABASE_SERVICE_ROLE_KEY`, `STRIPE_SECRET_KEY`, `CRON_SECRET` только для Production

### Шаг 4: Настройка домена

1. В настройках проекта перейдите в **Settings → Domains**
2. Добавьте домен: `moodflow-six.vercel.app`
3. Vercel автоматически настроит DNS записи
4. Если домен уже создан, он автоматически подключится

### Шаг 5: Настройка Cron Jobs

Cron jobs уже настроены в `vercel.json`. Убедитесь, что:

1. В Supabase добавлен URL для webhook: `https://moodflow-six.vercel.app/api/cron/weekly-summaries`
2. Для защиты cron endpoints используйте `CRON_SECRET` в коде

### Шаг 6: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения билда
3. После успешного деплоя проект будет доступен на `https://moodflow-six.vercel.app`

## 🔧 Способ 2: Деплой через Vercel CLI

### Шаг 1: Установка Vercel CLI

```bash
npm i -g vercel
```

### Шаг 2: Логин в Vercel

```bash
vercel login
```

### Шаг 3: Деплой

```bash
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow
vercel
```

Следуйте инструкциям:
- Выберите проект или создайте новый
- Укажите корневую директорию: `moodflow` (если нужно)
- Подтвердите настройки

### Шаг 4: Настройка переменных окружения через CLI

```bash
vercel env add NEXT_PUBLIC_SUPABASE_URL
vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY
vercel env add SUPABASE_SERVICE_ROLE_KEY
vercel env add NEXT_PUBLIC_APP_URL
# ... и так далее для всех переменных
```

### Шаг 5: Продакшен деплой

```bash
vercel --prod
```

## ✅ Проверка после деплоя

1. **Проверьте главную страницу**: `https://moodflow-six.vercel.app`
2. **Проверьте авторизацию**: попробуйте зарегистрироваться/войти
3. **Проверьте API endpoints**: убедитесь, что они работают
4. **Проверьте Supabase настройки**:
   - В Supabase Dashboard → Authentication → URL Configuration
   - Добавьте `https://moodflow-six.vercel.app` в **Site URL**
   - Добавьте в **Redirect URLs**:
     ```
     https://moodflow-six.vercel.app/auth/callback
     https://moodflow-six.vercel.app/api/auth/callback
     ```

## 🔒 Настройка Supabase для Production

### Важно! Обновите настройки в Supabase:

1. **Settings → API → URL Configuration**:
   - Site URL: `https://moodflow-six.vercel.app`
   
2. **Authentication → URL Configuration**:
   - Site URL: `https://moodflow-six.vercel.app`
   - Redirect URLs: добавьте:
     ```
     https://moodflow-six.vercel.app/auth/callback
     https://moodflow-six.vercel.app/api/auth/callback
     https://moodflow-six.vercel.app/**
     ```

3. **Database → Connection Pooling** (опционально):
   - Используйте connection pooling для production нагрузок

## 🐛 Возможные проблемы

### Ошибка "Invalid Supabase URL"
- Проверьте, что `NEXT_PUBLIC_SUPABASE_URL` правильно задан в Environment Variables
- Убедитесь, что переменная доступна для всех окружений

### Ошибки авторизации
- Проверьте настройки Redirect URLs в Supabase
- Убедитесь, что `NEXT_PUBLIC_APP_URL` указывает на правильный домен

### Cron jobs не работают
- Проверьте, что `CRON_SECRET` задан
- Убедитесь, что endpoints защищены проверкой секрета

### Проблемы с билдом
- Проверьте логи билда в Vercel Dashboard
- Убедитесь, что все зависимости установлены
- Проверьте TypeScript ошибки: `npm run build` локально

## 📝 Автоматический деплой

После настройки, каждый push в основную ветку будет автоматически деплоиться на production.

Для других веток будут создаваться preview deployments.

## 🔄 Обновление переменных окружения

После изменения переменных окружения:
1. Зайдите в **Settings → Environment Variables** в Vercel
2. Измените или добавьте переменную
3. Нажмите **"Redeploy"** для применения изменений

---

**Готово! Ваше приложение должно быть доступно на https://moodflow-six.vercel.app** 🎉

