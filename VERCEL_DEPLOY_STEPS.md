# 🚀 Шаги для успешного деплоя на Vercel

## ✅ Что уже сделано правильно:
- Root Directory пустой (правильно!)
- `vercel.json` настроен с явными командами
- `package.json` содержит Next.js
- Все файлы закоммичены и запушены

## 📋 Следующие шаги:

### 1. Перезапустите деплой в Vercel

После изменения Root Directory **обязательно** нужно перезапустить деплой:

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект **moodflow**
3. Перейдите на вкладку **Deployments**
4. Найдите последний деплой (должен быть с ошибкой)
5. Нажмите на три точки (⋮) рядом с деплоем
6. Выберите **Redeploy**
7. Дождитесь завершения билда

### 2. Проверьте настройки Framework Preset

Убедитесь, что Framework Preset правильно настроен:

1. В Vercel Dashboard → **Settings** → **General**
2. Найдите секцию **Framework Preset**
3. Должно быть выбрано **Next.js**
4. Если нет - выберите **Next.js** и сохраните

### 3. Проверьте переменные окружения

Убедитесь, что все необходимые переменные окружения добавлены:

1. В Vercel Dashboard → **Settings** → **Environment Variables**
2. Проверьте наличие:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - И другие необходимые переменные

### 4. Если деплой все еще не работает

#### Вариант A: Очистите кэш и передеплойте

1. В Vercel Dashboard → **Deployments**
2. Создайте новый деплой через **Redeploy**
3. Или закоммитьте пустой коммит для триггера нового деплоя:
   ```bash
   git commit --allow-empty -m "Trigger Vercel redeploy"
   git push
   ```

#### Вариант B: Используйте Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Перейдите в директорию проекта
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow

# Задеплойте проект
vercel --prod
```

При запросе:
- **Set up and deploy?** → Yes
- **Which scope?** → Выберите ваш аккаунт
- **Link to existing project?** → Yes
- **What's the name of your existing project?** → moodflow
- **In which directory is your code located?** → `.` (точка)

#### Вариант C: Переподключите проект

Если ничего не помогает:

1. В Vercel Dashboard удалите текущий проект
2. Создайте новый проект через **Add New Project**
3. Импортируйте ваш GitHub репозиторий
4. **Важно:** Убедитесь, что Root Directory **пустой**
5. Framework Preset должен быть **Next.js**
6. Добавьте все переменные окружения
7. Нажмите **Deploy**

## 🔍 Проверка логов

Если деплой все еще не работает, проверьте логи:

1. В Vercel Dashboard → **Deployments**
2. Выберите деплой с ошибкой
3. Нажмите **View Function Logs** или **View Build Logs**
4. Ищите конкретные ошибки в логах

## 📝 Текущая конфигурация

- ✅ Root Directory: пустой (правильно)
- ✅ Framework: Next.js (должен быть в настройках)
- ✅ Build Command: `npm run build` (в vercel.json)
- ✅ Install Command: `npm install` (в vercel.json)
- ✅ Output Directory: `.next` (в vercel.json)

## 🆘 Если проблема сохраняется

Пришлите скриншот или текст ошибки из логов Vercel, чтобы можно было точно определить проблему.


