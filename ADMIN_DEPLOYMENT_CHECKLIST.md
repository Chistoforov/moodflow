# Чеклист развертывания админ-панели

## ✅ Что нужно сделать

### 1. Запустить миграцию базы данных

Откройте Supabase Dashboard → SQL Editor и выполните:

```sql
-- Содержимое файла: supabase/migrations/019_add_initial_admin.sql
```

**Скопируйте и выполните весь код из файла `supabase/migrations/019_add_initial_admin.sql`**

Эта миграция:
- Добавит `site4people@gmail.com` в таблицу `psychologists` с ролью `admin`
- Создаст триггер, который автоматически даст права админа при первом входе через Google SSO
- Обновит существующую запись, если она уже есть

### 2. Задеплоить приложение

```bash
# Установить новые зависенности
npm install

# Собрать проект
npm run build

# Задеплоить на Vercel (или ваш хостинг)
vercel --prod
```

### 3. Войти как администратор

1. Откройте ваш домен
2. Если вы ещё не входили как `site4people@gmail.com`, выполните вход через Google SSO
3. Перейдите на `/admin` или `домен.ru/admin`

### 4. Проверить работу

Убедитесь что доступны все разделы:
- ✅ `/admin/dashboard` - Dashboard
- ✅ `/admin/users` - Управление пользователями
- ✅ `/admin/materials` - Управление материалами

### 5. Проверить создание материала

1. Откройте `/admin/materials`
2. Нажмите зелёную кнопку с плюсом
3. Заполните:
   - Заголовок
   - Текст (используя WYSIWYG редактор)
   - Опционально: дату публикации
4. Сохраните
5. Убедитесь, что материал появился в списке

## 🎯 Доступы

### Кто может заходить в админку?
- Только пользователи с `role = 'admin'` в таблице `psychologists`
- Изначально: только `site4people@gmail.com`

### Как добавить ещё одного администратора?
1. Войдите в админку как `site4people@gmail.com`
2. Откройте `/admin/users`
3. Найдите нужного пользователя
4. Выберите в выпадающем списке "Админ"

## 🔐 Безопасность

- ✅ Middleware проверяет роль перед доступом к `/admin/*`
- ✅ API эндпоинты проверяют роль администратора
- ✅ Неавторизованные пользователи редиректятся на `/login`
- ✅ Пользователи без прав админа редиректятся на главную

## 📝 Примечания

1. **Первый вход**: При первом входе `site4people@gmail.com` через Google SSO, триггер автоматически добавит пользователя в таблицу `psychologists` с ролью `admin`

2. **React Quill**: Установлен с флагом `--legacy-peer-deps` для совместимости с React 19

3. **Роли пользователей**:
   - `free` - бесплатный доступ
   - `subscription` - платная подписка
   - `personal` - личный психолог
   - `admin` - администратор (добавляется в таблицу `psychologists`)

## 🐛 Устранение проблем

### Не могу войти в админку
1. Убедитесь, что выполнили миграцию `019_add_initial_admin.sql`
2. Проверьте, что вошли как `site4people@gmail.com`
3. Проверьте в Supabase, что в таблице `psychologists` есть запись с вашим email и `role = 'admin'`

### WYSIWYG редактор не работает
1. Убедитесь, что установлены пакеты: `react-quill` и `quill`
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что страница загружается на клиенте (не SSR)

### Не сохраняются материалы
1. Проверьте консоль браузера на ошибки
2. Проверьте, что API эндпоинты `/api/admin/posts` доступны
3. Убедитесь, что заполнены обязательные поля (заголовок и текст)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте логи Vercel/хостинга
3. Проверьте логи Supabase (Dashboard → Logs)

