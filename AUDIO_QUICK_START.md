# Быстрый старт: Аудио-записи

## 1. Добавьте переменные окружения

В файл `.env.local` добавьте:

```bash
# Для транскрипции аудио (обязательно)
OPENAI_API_KEY=sk-...

# Должны быть уже настроены
NEXT_PUBLIC_SUPABASE_URL=your-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-key
```

## 2. Выполните миграции

```bash
cd moodflow

# Если используете локальный Supabase
npx supabase db reset

# Если используете удаленный Supabase
npx supabase db push
```

Это создаст:
- ✅ Поле `processing_status` в таблице `daily_entries`
- ✅ Storage bucket `audio-entries`
- ✅ RLS политики для безопасного доступа

## 3. Запустите приложение

```bash
npm run dev
```

## 4. Проверьте работу

1. Откройте http://localhost:3000
2. Войдите в систему
3. Откройте любую запись дня (например, `/entry/2025-11-04`)
4. В секции "Заметки" нажмите **"Записать аудио"**
5. Разрешите доступ к микрофону
6. Запишите что-нибудь (хотя бы 10 секунд)
7. Нажмите кнопку остановки (квадрат)
8. Должно появиться сообщение: **"Аудио обрабатывается..."**
9. Подождите ~30-60 секунд
10. Текст появится в поле заметок автоматически

## Готово! 🎉

Теперь пользователи могут записывать голосовые заметки, которые автоматически превращаются в текст.

### Что дальше?

Полная документация: [AUDIO_ENTRIES_SETUP.md](./AUDIO_ENTRIES_SETUP.md)

### Возможные проблемы

**"Микрофон не работает"**
- Проверьте разрешения браузера
- Используйте HTTPS или localhost

**"Ошибка обработки аудио"**
- Проверьте OPENAI_API_KEY
- Проверьте баланс OpenAI аккаунта

**"Ошибка загрузки"**
- Проверьте, что миграции выполнены
- Проверьте Storage в Supabase Dashboard





