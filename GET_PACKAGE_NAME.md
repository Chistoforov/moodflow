# 📦 Как получить Package Name и SHA-256 для assetlinks.json

## Что такое Package Name?

Package Name - это уникальный идентификатор вашего Android приложения, например:
- `com.example.moodflow`
- `app.vercel.moodflow_six.twa`

## 🔍 Способ 1: Через Google Play Console (Рекомендуется)

### Если приложение уже опубликовано:

1. Откройте [Google Play Console](https://play.google.com/console)
2. Выберите ваше приложение
3. В левом меню найдите **App content** или **Dashboard**
4. Package name будет отображаться вверху страницы

### Получение SHA-256 fingerprint:

1. В Google Play Console выберите приложение
2. Перейдите в **Release → Setup → App integrity**
3. Найдите секцию **App signing key certificate**
4. Скопируйте **SHA-256 certificate fingerprint**
5. Формат: `AA:BB:CC:DD:...` (длинная строка с двоеточиями)

---

## 🔍 Способ 2: Через Google Play Studio / Bubblewrap

Если вы создавали APK через Google Play Studio или Bubblewrap:

### Package Name:

1. Откройте проект в Google Play Studio
2. Package name указывается при создании проекта
3. Обычно это: `app.vercel.ВАШЕ_ПРИЛОЖЕНИЕ.twa`

Для вашего случая вероятно:
```
app.vercel.moodflow_six.twa
```

### SHA-256 fingerprint:

1. Найдите файл `assetlinks.json` в настройках проекта
2. Или используйте команду:

```bash
keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
```

---

## 🔍 Способ 3: Через установленное APK

Если APK уже установлен на устройстве:

### На Android устройстве:

1. Установите приложение **App Inspector** или **Package Name Viewer**
2. Найдите ваше приложение в списке
3. Посмотрите Package Name

### Через ADB (если устройство подключено к компьютеру):

```bash
# Получить список всех package names
adb shell pm list packages | grep moodflow

# Получить информацию о конкретном приложении
adb shell dumpsys package com.your.package.name
```

---

## 📝 Обновление assetlinks.json

После получения Package Name и SHA-256:

1. Откройте файл:
   ```
   /Users/d.chistoforov/moodflow/public/.well-known/assetlinks.json
   ```

2. Замените значения:
   ```json
   [
     {
       "relation": ["delegate_permission/common.handle_all_urls"],
       "target": {
         "namespace": "android_app",
         "package_name": "ВАШ_PACKAGE_NAME",
         "sha256_cert_fingerprints": [
           "AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99:AA:BB:CC:DD:EE:FF:00:11:22:33:44:55:66:77:88:99"
         ]
       }
     }
   ]
   ```

3. Задеплойте на Vercel:
   ```bash
   git add public/.well-known/assetlinks.json
   git commit -m "Update assetlinks.json with correct package name"
   git push
   ```

4. Проверьте доступность:
   ```
   https://moodflow-six.vercel.app/.well-known/assetlinks.json
   ```

---

## ⚠️ Важно!

### Для TWA (Trusted Web Activity):

Package name обычно формируется автоматически:
```
app.vercel.ДОМЕН_БЕЗ_ТОЧЕК.twa
```

Для `moodflow-six.vercel.app` это будет:
```
app.vercel.moodflow_six.twa
```

### SHA-256 fingerprint:

- Для **debug build**: используйте debug keystore
- Для **release build**: используйте production keystore из Google Play Console
- **Важно**: SHA-256 должен соответствовать тому keystore, которым подписан APK

---

## 🧪 Проверка правильности

### Проверка через Google:

1. Откройте https://developers.google.com/digital-asset-links/tools/generator
2. Введите ваш домен: `moodflow-six.vercel.app`
3. Введите package name
4. Введите SHA-256 fingerprint
5. Нажмите **Generate Statement**
6. Сравните с вашим assetlinks.json

### Проверка через ADB:

```bash
adb shell pm get-app-links com.your.package.name
```

Должно показать verified links для вашего домена.

---

## ❓ Нужна ли эта настройка?

### ✅ Нужна, если:
- Вы хотите использовать Android App Links
- Хотите, чтобы ссылки открывались напрямую в приложении
- Хотите улучшить UX для OAuth редиректов

### ❌ Не обязательна, если:
- Вы используете polling механизм (уже реализован)
- Пользователи готовы вручную возвращаться в приложение после OAuth
- Приложение работает как PWA в браузере

---

## 📧 Если не получается

Текущая реализация OAuth **работает без assetlinks.json**!

Polling механизм автоматически определит авторизацию, когда пользователь вернется в приложение.

assetlinks.json - это **опциональное улучшение** для более плавного UX.

