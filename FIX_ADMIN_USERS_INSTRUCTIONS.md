# 🔧 Исправление проблем с админ-панелью пользователей

## Проблемы
1. ❌ Админ видит только себя в списке пользователей
2. ❌ Роль "Админ" отображается как "Бесплатный"

## ✅ Решение применено

### Что было исправлено:

1. **Backend API** (`/src/app/api/admin/users/route.ts`)
   - Теперь возвращает `effective_role` - правильную роль пользователя
   - Проверяет таблицу `psychologists` для определения админов

2. **Frontend** (`/src/app/admin/users/page.tsx`)
   - Использует `effective_role` для отображения роли
   - Правильно показывает роль "Админ"

3. **База данных** (миграция `022_add_admin_users_policy.sql`)
   - Добавлены RLS политики для админов
   - Админы теперь могут видеть всех пользователей

## 📝 Как применить исправления

### Шаг 1: Применить миграцию базы данных

Откройте файл `QUICK_FIX_ADMIN_USERS.sql` и выполните его через Supabase Dashboard:

1. Перейдите в Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект MoodFlow
3. Перейдите в **SQL Editor** (слева в меню)
4. Создайте **New Query**
5. Скопируйте и вставьте содержимое файла `QUICK_FIX_ADMIN_USERS.sql`
6. Нажмите **Run** или **Cmd/Ctrl + Enter**

### Шаг 2: Проверить исправление

1. Перезагрузите страницу админ-панели
2. Перейдите в раздел **"Пользователи"**
3. Теперь вы должны видеть:
   - ✅ Всех зарегистрированных пользователей
   - ✅ Вашу роль как "Админ" (не "Бесплатный")

## 🎯 Ожидаемый результат

После применения исправлений:
- Вы увидите всех пользователей в списке (включая тестового пользователя с другого email)
- Ваша роль будет отображаться как "Админ"
- Вы сможете изменять роли других пользователей через выпадающий список

## 🔍 Проверка (опционально)

Если хотите проверить, что политики применились, выполните этот SQL запрос в Supabase Dashboard:

```sql
SELECT 
  tablename,
  policyname,
  permissive,
  cmd
FROM pg_policies 
WHERE tablename = 'users'
ORDER BY policyname;
```

Вы должны увидеть политики:
- `Admins can view all users`
- `Admins can update all users`
- `Users can view own data`
- `Users can update own data`

## 💡 Дополнительная информация

Подробное описание изменений смотрите в файле `APPLY_MIGRATION_022.md`.

