# 🚀 Инструкция по установке и запуску MoodFlow

## ✅ Текущий статус

Проект полностью настроен и готов к работе! Все файлы созданы, зависимости установлены.

## 📋 Что уже сделано

- ✅ Установлены все необходимые зависимости
- ✅ Создана структура проекта
- ✅ Настроены конфигурационные файлы
- ✅ Созданы все роуты (auth, user, admin, api)
- ✅ Созданы компоненты и страницы
- ✅ Настроены интеграции (Supabase, Perplexity, Telegram, Stripe)
- ✅ Создана SQL миграция для базы данных
- ✅ Проект успешно собирается

## 🔧 Что нужно сделать для запуска

### 1. Настроить переменные окружения

Создайте файл `.env.local` в корне проекта `/Users/d.chistoforov/Desktop/MoodFlow/moodflow/`:

```bash
# Supabase (обязательно)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Perplexity (опционально для старта)
PERPLEXITY_API_KEY=your-perplexity-key

# Telegram (опционально для старта)
TELEGRAM_BOT_TOKEN=your-bot-token

# Stripe (опционально для старта)
STRIPE_SECRET_KEY=sk_test_your-key
STRIPE_WEBHOOK_SECRET=whsec_your-secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-key
STRIPE_PRICE_SUBSCRIPTION=price_subscription_id
STRIPE_PRICE_PERSONAL=price_personal_id

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
CRON_SECRET=your-random-secret-for-cron
```

### 2. Настроить Supabase

#### 2.1 Создать проект

1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Дождитесь завершения создания (2-3 минуты)

#### 2.2 Получить ключи

1. Перейдите в Settings → API
2. Скопируйте:
   - `URL` → `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public` → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `service_role` → `SUPABASE_SERVICE_ROLE_KEY`

#### 2.3 Запустить миграцию

1. В Supabase перейдите в SQL Editor
2. Создайте новый query
3. Скопируйте содержимое файла `supabase/migrations/001_initial_schema.sql`
4. Вставьте в редактор и нажмите Run

#### 2.4 Настроить Authentication

1. Перейдите в Authentication → Providers
2. Включите Email provider
3. В Email Templates можно настроить шаблоны писем (опционально)

### 3. Запустить проект

```bash
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

### 4. Проверить работу

1. Перейдите на главную страницу
2. Нажмите "Войти"
3. Введите email
4. Проверьте почту и перейдите по ссылке
5. Вы будете перенаправлены в календарь настроения

## 🎯 Основные маршруты

### Публичные
- `/` - Главная страница с тарифами
- `/login` - Страница входа

### Для пользователей (требуется авторизация)
- `/calendar` - Календарь настроения
- `/entry/[date]` - Создание/редактирование записи
- `/recommendations` - Рекомендации
- `/profile` - Профиль и подписка

### Для администраторов (требуется роль психолога)
- `/dashboard` - Статистика
- `/users` - Список пользователей

### API
- `/api/auth/callback` - OAuth callback
- `/api/entries` - CRUD для записей
- `/api/recommendations` - Получение рекомендаций
- `/api/cron/weekly-summaries` - Cron для еженедельных отчетов

## 🔐 Создание администратора

После создания первого пользователя через email, вручную добавьте запись в таблицу `psychologists`:

```sql
INSERT INTO psychologists (user_id, email, full_name, role, active)
VALUES (
  'uuid-вашего-пользователя', 
  'admin@example.com', 
  'Администратор', 
  'admin', 
  true
);
```

UUID пользователя можно найти в таблице `users` в колонке `sso_uid`.

## 📦 Опциональные интеграции

### Perplexity AI (для ИИ-анализа)

1. Зарегистрируйтесь на [perplexity.ai](https://www.perplexity.ai)
2. Получите API ключ
3. Добавьте в `.env.local`

### Telegram Bot (для уведомлений)

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте токен
4. Добавьте в `.env.local`

### Stripe (для оплаты)

1. Создайте аккаунт на [stripe.com](https://stripe.com)
2. Создайте Products для подписок (990₽ и 4990₽)
3. Получите Price IDs
4. Скопируйте API ключи из Dashboard → Developers
5. Добавьте все в `.env.local`

## 🚀 Деплой на Vercel

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Войдите в аккаунт
vercel login

# 3. Задеплойте проект
vercel

# 4. Добавьте переменные окружения
vercel env add NEXT_PUBLIC_SUPABASE_URL
vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY
# ... и так далее
```

## 📊 Структура базы данных

Основные таблицы:
- `users` - Пользователи
- `psychologists` - Психологи и администраторы
- `daily_entries` - Дневниковые записи
- `weekly_summaries` - Еженедельные отчеты
- `recommendations` - Рекомендации от психологов
- `conversations` & `messages` - Чаты (для Personal tier)
- `posts` - Статьи и видео
- `subscriptions` - Подписки Stripe
- `notifications` - Уведомления

## 🐛 Решение проблем

### Ошибка "Supabase URL not found"

Убедитесь, что в `.env.local` правильно указаны:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

Перезапустите dev server: `npm run dev`

### Ошибка при миграции базы данных

1. Проверьте, что у вас есть права на создание таблиц
2. Убедитесь, что extension `uuid-ossp` включен
3. Выполните миграцию частями, если есть ошибки

### Не работает авторизация

1. Проверьте настройки Email provider в Supabase
2. Убедитесь, что в Authentication → URL Configuration правильно указан Site URL
3. Добавьте `http://localhost:3000/**` в Redirect URLs

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи Supabase в Dashboard
3. Создайте Issue в репозитории

## ✨ Следующие шаги

После запуска проекта вы можете:
1. Настроить UI компоненты из shadcn/ui
2. Добавить обработку аудио-записей
3. Интегрировать Perplexity для ИИ-анализа
4. Настроить Stripe для приема платежей
5. Добавить Telegram уведомления
6. Создать административную панель для психологов

---

**Проект готов к запуску! 🎉**

Удачи в разработке MoodFlow!

