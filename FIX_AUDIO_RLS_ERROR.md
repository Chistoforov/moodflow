# 🔧 Исправление ошибки RLS при отправке аудио

## 🐛 Проблема

При отправке аудио возникает ошибка:
```
new row violates row-level security policy
```

**Причина:** Политики RLS (Row Level Security) для таблицы `audio_entries` не работают корректно при вставке данных с сервера. Проблема в том, что `auth.uid()` может быть не установлен правильно в контексте серверных API routes.

## ✅ Решение

### Шаг 1: Применить миграцию 008 (для daily_entries)

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (в левом меню)
4. Создайте новый query и скопируйте весь код из файла:
   ```
   supabase/migrations/008_fix_daily_entries_rls.sql
   ```
5. Нажмите **Run** для выполнения

### Шаг 2: Применить миграцию 009 (для audio_entries) - ОБЯЗАТЕЛЬНО!

1. В том же **SQL Editor** создайте новый query
2. Скопируйте весь код из файла:
   ```
   supabase/migrations/009_fix_audio_entries_rls.sql
   ```
3. Нажмите **Run** для выполнения

### Шаг 3: Проверка

После применения обеих миграций попробуйте отправить аудио снова. Ошибка должна исчезнуть.

## 🔍 Что было исправлено:

1. **Для таблицы `daily_entries` (миграция 008):**
   - Создана вспомогательная функция `user_owns_daily_entry()` с `SECURITY DEFINER`
   - Функция обходит проблемы с подзапросами в RLS политиках
   - Политики пересозданы с использованием этой функции

2. **Для таблицы `audio_entries` (миграция 009):**
   - Создана вспомогательная функция `user_owns_audio_entry()` с `SECURITY DEFINER`
   - Функция проверяет, что `auth.uid()` соответствует `user_id` в таблице `audio_entries`
   - Политики пересозданы с использованием этой функции
   - Это решает проблему с `auth.uid()`, который может быть NULL или не работать правильно в серверном контексте

## 💡 Технические детали:

- `SECURITY DEFINER` позволяет функции обходить RLS и правильно проверять контекст аутентификации
- Функция `user_owns_audio_entry()` проверяет, что `auth.uid()` соответствует `user_id` записи
- Это решает проблему, когда `auth.uid()` не работает корректно при выполнении запросов с сервера через API routes

## ⚠️ Важно:

Если ошибка сохраняется после применения миграций:

1. Убедитесь, что обе миграции (008 и 009) применены успешно
2. Убедитесь, что пользователь авторизован (есть активная сессия)
3. Проверьте, что пользователь существует в таблице `users` (должен быть создан автоматически через trigger)
4. Проверьте логи в Supabase Dashboard → Logs → Postgres Logs
5. Убедитесь, что функция `user_owns_audio_entry` создана и имеет правильные права доступа

