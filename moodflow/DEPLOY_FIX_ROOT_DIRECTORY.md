# 🔧 Исправление ошибки деплоя на Vercel

## ❌ Проблема

```
Error: > Couldn't find any `pages` or `app` directory. Please create one under the project root
```

## 🔍 Причина

Корень git-репозитория находится в `moodflow/`, что правильно. Root Directory в Vercel должна быть **пустой** (это верно).

Проблема в том, что:
1. Изменения в `package.json` и `vercel.json` не закоммичены/не запушены
2. Vercel использует старую версию кода без скрипта `vercel-build`

## ✅ Решение

### Шаг 1: Закоммитьте и запушьте изменения

**Это самое важное!** Vercel использует код из git, поэтому все изменения должны быть запушены:

```bash
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow

# Проверьте статус
git status

# Добавьте все изменения
git add package.json vercel.json .vercelignore

# Закоммитьте
git commit -m "Fix Vercel deployment: add vercel-build script"

# Запушьте в репозиторий
git push
```

После push Vercel автоматически запустит новый деплой с обновленным кодом.

### Шаг 2: Убедитесь, что Root Directory пустая

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект **moodflow**
3. Перейдите в **Settings** → **General**
4. Найдите секцию **Root Directory**
5. **Убедитесь, что поле ПУСТОЕ** (или содержит `.`)
6. Если там что-то указано - очистите и сохраните

### Шаг 3: Перезапустите деплой (если нужно)

После изменения Root Directory **обязательно** перезапустите деплой:

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой
3. Нажмите три точки (⋮) → **Redeploy**
4. Дождитесь завершения билда

## 📝 Что было исправлено в коде

1. ✅ Добавлен скрипт `vercel-build` в `package.json`
2. ✅ Обновлен `vercel.json` с правильной командой билда
3. ✅ Создан `.vercelignore` для правильной работы с Vercel

## 🎯 Альтернатива: Деплой через CLI

Если настройка через Dashboard не помогает:

```bash
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow
vercel --prod
```

При запросе:
- **In which directory is your code located?** → `.` (точка, так как вы уже в директории moodflow)

## ✅ Проверка

После правильной настройки Root Directory Vercel должен:
- ✅ Найти `package.json` в `moodflow/`
- ✅ Найти `src/app/` в `moodflow/src/app/`
- ✅ Успешно выполнить билд

