# 🔧 Окончательное решение проблемы RLS при отправке аудио

## 🐛 Проблема

После применения миграций 008 и 009 ошибка все еще сохраняется:
```
new row violates row-level security policy
```

**Причина:** SSR клиент Supabase может не передавать JWT токен правильно в RLS политики при выполнении запросов из серверных API routes. Функции с `auth.uid()` возвращают NULL даже когда пользователь аутентифицирован.

## ✅ Решение: Использование функции для вставки

Создана миграция **013**, которая создает функцию `insert_audio_entry` с `SECURITY DEFINER`. Эта функция:
- Обходит проблемы с RLS, так как выполняется с правами создателя
- Проверяет, что `user_id` соответствует аутентифицированному пользователю
- Безопасно вставляет запись в таблицу

Код API route также обновлен для использования этой функции.

## 📋 Инструкция по применению

### Шаг 1: Применить миграцию 013

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Создайте новый query и скопируйте весь код из файла:
   ```
   supabase/migrations/013_fix_audio_entries_insert_function.sql
   ```
5. Нажмите **Run** для выполнения

### Шаг 2: Проверка

После применения миграции попробуйте отправить аудио снова. Ошибка должна исчезнуть.

## 🔍 Что было сделано:

### Миграция 013:
1. **Создана функция `insert_audio_entry`**:
   - Использует `SECURITY DEFINER` для обхода RLS
   - Проверяет, что `auth.uid()` соответствует переданному `user_id`
   - Безопасно вставляет запись в таблицу `audio_entries`
   - Возвращает ID созданной записи

2. **Обновлены RLS политики**:
   - Удалена политика INSERT (вставка теперь через функцию)
   - Оставлены политики для SELECT, UPDATE, DELETE

### Обновление кода:
- Файл `src/app/api/upload-audio/route.ts` обновлен для использования функции `insert_audio_entry`
- Добавлен fallback на прямой INSERT, если функция не существует (для обратной совместимости)

## 💡 Технические детали:

- `SECURITY DEFINER` позволяет функции выполниться с правами создателя функции, обходя RLS
- Функция проверяет `auth.uid()` внутри себя, что работает правильно даже в серверном контексте
- Если `auth.uid()` не соответствует переданному `user_id`, функция выбрасывает ошибку
- Это безопасный подход, так как проверка все равно выполняется

## ⚠️ Важно:

1. **Примените миграцию 013** - это обязательно
2. После применения миграции код автоматически начнет использовать функцию
3. Если функция не существует, код использует fallback на прямой INSERT
4. Убедитесь, что пользователь аутентифицирован перед отправкой аудио

## 🔍 Диагностика:

Если ошибка все еще сохраняется:

1. Проверьте, что миграция применена:
   ```sql
   SELECT proname FROM pg_proc WHERE proname = 'insert_audio_entry';
   ```

2. Проверьте права доступа:
   ```sql
   SELECT grantee, privilege_type 
   FROM information_schema.routine_privileges 
   WHERE routine_name = 'insert_audio_entry';
   ```

3. Проверьте логи в Supabase Dashboard → Logs → Postgres Logs

4. Проверьте, что пользователь аутентифицирован (есть активная сессия)

5. Проверьте консоль браузера и серверные логи для деталей ошибки

## 🚀 Альтернативные решения (если миграция 013 не помогла):

Если проблема сохраняется, можно попробовать:

1. **Миграция 012** - упрощенные RLS политики без функций
2. **Миграция 011** - использование функции `get_current_user_id()`
3. **Миграция 010** - использование JWT claims с fallback

Но рекомендуется начать с миграции 013, так как это наиболее надежное решение.

