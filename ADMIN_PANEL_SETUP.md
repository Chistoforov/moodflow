# Настройка панели администратора MoodFlow

## ✅ Что было сделано

### 1. База данных
- Создана миграция для автоматического добавления `site4people@gmail.com` как администратора
- При первом входе через Google SSO этот пользователь автоматически получит права админа

### 2. Админ-панель доступна по адресу `/admin`

Включает 3 раздела:

#### Dashboard (`/admin/dashboard`)
- Главная страница админки
- Статистика (пока заглушки)

#### Пользователи (`/admin/users`)
- Список всех зарегистрированных пользователей
- Email и имя пользователя
- Выпадающий список для изменения роли:
  - **Бесплатный** - базовый доступ
  - **Подписка** - платная подписка
  - **Личный психолог** - доступ к личному психологу
  - **Админ** - полный доступ к админ-панели
- Дата регистрации

#### Материалы (`/admin/materials`)
- Список всех материалов (опубликованных и черновиков)
- Кнопка "+" для создания нового материала (зелёная)
- Для каждого материала:
  - Кнопка редактирования (карандаш)
  - Кнопка удаления (корзина)
  - Статус (опубликован/черновик)
  - Дата публикации

### 3. Модальное окно создания/редактирования материала

- **Заголовок** - текстовое поле
- **Текст** - WYSIWYG редактор (react-quill) с возможностью:
  - Заголовки (H1, H2, H3)
  - Жирный, курсив, подчеркивание, зачеркивание
  - Списки (маркированные и нумерованные)
  - Ссылки
- **Дата публикации** - опциональное поле
  - Если не заполнено - публикуется сразу
  - Если заполнено будущей датой - материал будет в черновиках до этой даты

### 4. Защита доступа
- Только пользователи с ролью "admin" в таблице `psychologists` могут заходить в `/admin`
- Все API эндпоинты `/api/admin/*` защищены проверкой роли
- Неавторизованные пользователи перенаправляются на `/login`
- Пользователи без прав админа перенаправляются на главную

## 🚀 Как развернуть

### 1. Выполните миграцию базы данных

В Supabase SQL Editor выполните:

```sql
-- Файл: supabase/migrations/019_add_initial_admin.sql
```

Скопируйте и выполните содержимое файла `supabase/migrations/019_add_initial_admin.sql`

### 2. Войдите в систему

1. Перейдите на ваш домен
2. Войдите через Google SSO используя email: `site4people@gmail.com`
3. После входа перейдите на `/admin`

### 3. Готово!

Теперь вы можете:
- Управлять пользователями и их ролями
- Создавать, редактировать и удалять материалы
- Планировать публикации на будущее

## 📋 API Эндпоинты

### Пользователи

- `GET /api/admin/users` - Получить всех пользователей
- `PATCH /api/admin/users/[userId]` - Обновить роль пользователя

### Материалы

- `GET /api/admin/posts` - Получить все материалы (включая черновики)
- `POST /api/admin/posts` - Создать новый материал
- `GET /api/admin/posts/[postId]` - Получить один материал
- `PATCH /api/admin/posts/[postId]` - Обновить материал
- `DELETE /api/admin/posts/[postId]` - Удалить материал

## 🎨 Дизайн

Админ-панель следует дизайн-системе приложения:
- Цветовая схема: `#8B3A3A` (бордовый), `#E8E2D5` (бежевый), `#F5F1EB` (светлый)
- Адаптирована для работы на десктопе
- Современный UI с плавными переходами

## 🔐 Безопасность

- Все запросы проверяют сессию пользователя
- Проверка роли администратора на уровне API
- Middleware защищает роуты на уровне приложения
- RLS политики Supabase обеспечивают защиту данных

## 📝 Примечания

1. **Роль "Админ" в селекторе ролей** - при выборе этой роли пользователь добавляется в таблицу `psychologists` с `role='admin'` и получает доступ к админ-панели

2. **WYSIWYG редактор** - используется react-quill, совместимый с React 19 через `--legacy-peer-deps`

3. **Отложенная публикация** - материалы с датой публикации в будущем автоматически помечаются как черновики до наступления указанной даты

4. **Slug генерация** - URL материалов автоматически генерируются из заголовка

## 🐛 Известные особенности

- Если два материала имеют одинаковый заголовок, к slug автоматически добавляется timestamp
- React-quill установлен с флагом `--legacy-peer-deps` для совместимости с React 19
- Первый вход администратора должен быть выполнен через Google SSO для автоматической регистрации

## 🔄 Следующие шаги (опционально)

Вы можете расширить функциональность:
- Добавить категории для материалов
- Добавить загрузку изображений
- Добавить видео-материалы
- Добавить аналитику и статистику в Dashboard
- Добавить поиск и фильтры для пользователей и материалов

