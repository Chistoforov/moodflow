# 🔧 Исправление ошибки деплоя на Vercel

## ❌ Ошибки

### Ошибка 1: Не найдена директория app
```
Error: > Couldn't find any `pages` or `app` directory. Please create one under the project root
```

### Ошибка 2: Не найден Next.js
```
Error: No Next.js version detected. Make sure your package.json has "next" in either "dependencies" or "devDependencies".
Warning: Could not identify Next.js version, ensure it is defined as a project dependency.
```

## 🔍 Причина
Обе ошибки возникают из-за того, что **Root Directory в Vercel настроен неправильно**. Vercel ищет `package.json` и директорию `app` не в той директории, где они находятся.

Ваш проект находится в корне репозитория (`moodflow/`), но Vercel может быть настроен на поиск в другой директории.

## ✅ Решение

### Шаг 1: Настройка Root Directory в Vercel Dashboard (КРИТИЧЕСКИ ВАЖНО! ⚠️)

**Это самое важное действие!** Без правильной настройки Root Directory деплой не будет работать.

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект **moodflow**
3. Перейдите в **Settings** (⚙️)
4. Прокрутите вниз до секции **Root Directory**
5. **ВАЖНО:** Убедитесь, что поле **пустое** (оставьте пустым) или содержит `.` (точка)
   - Ваш репозиторий уже содержит только проект `moodflow/`
   - Root Directory должен быть **пустым** или `.` (не `moodflow`!)
6. Нажмите **Save**
7. **Обязательно** перезапустите деплой после изменения Root Directory

### Шаг 2: Исправление lockfile (если нужно)

Если в логах есть предупреждение о lockfile, выполните локально:

```bash
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow
npm install
npm run build
```

Затем закоммитьте обновленный `package-lock.json`:

```bash
git add package-lock.json
git commit -m "Update package-lock.json"
git push
```

### Шаг 3: Передеплой

1. В Vercel Dashboard перейдите на вкладку **Deployments**
2. Найдите последний деплой
3. Нажмите **Redeploy** (три точки → Redeploy)
4. Дождитесь завершения билда

## 🎯 Альтернативное решение: Деплой через Vercel CLI

Если настройка через Dashboard не помогает, используйте CLI:

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Перейдите в директорию проекта
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow

# Задеплойте проект
vercel --prod
```

При первом запуске CLI спросит:
- **Set up and deploy?** → Yes
- **Which scope?** → Выберите ваш аккаунт
- **Link to existing project?** → Yes
- **What's the name of your existing project?** → moodflow
- **In which directory is your code located?** → `.` (точка, текущая директория)

## 📝 Проверка структуры проекта

Убедитесь, что в корне проекта есть:
- ✅ `package.json`
- ✅ `next.config.ts`
- ✅ `src/app/` (директория с Next.js приложением)
- ✅ `vercel.json` (опционально, для cron jobs)

## 🆘 Если всё ещё не работает

1. Проверьте логи деплоя в Vercel Dashboard → Deployments → выберите деплой → View Function Logs
2. Убедитесь, что все переменные окружения настроены
3. Попробуйте удалить проект в Vercel и создать заново с правильным Root Directory

