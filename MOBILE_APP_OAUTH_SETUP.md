# 📱 Настройка OAuth для мобильного приложения (APK)

## Проблема

Когда приложение работает как APK (созданное через Google Play Studio / TWA), после OAuth логина через Google браузер не возвращает пользователя обратно в приложение, и приложение висит на экране "Вход...".

## Решение

Мы реализовали **polling-based auth flow** для мобильных устройств:
1. Приложение открывает браузер для OAuth
2. После успешного входа браузер показывает сообщение "Закройте окно и вернитесь в приложение"
3. Приложение автоматически проверяет статус авторизации каждые 2 секунды
4. Когда авторизация завершена, приложение автоматически перенаправляет на календарь

---

## 🔧 Что было сделано

### 1. Обновлен OAuth flow (`src/app/(auth)/login/page.tsx`)
- Добавлено детектирование мобильных устройств и PWA/TWA режима
- Реализован polling механизм для проверки авторизации каждые 2 секунды
- Добавлено информативное сообщение для пользователя

### 2. Улучшен callback handler (`src/app/(auth)/callback/page.tsx`)
- Добавлена обработка ошибок OAuth
- Для мобильных устройств показывается инструкция закрыть браузер
- Попытка автоматически закрыть окно браузера
- Fallback на редирект, если не получается закрыть окно

### 3. Создан API endpoint для проверки статуса (`src/app/api/auth/status/route.ts`)
- Позволяет приложению проверять статус авторизации без полной перезагрузки

### 4. Добавлен assetlinks.json (`public/.well-known/assetlinks.json`)
- Для Android App Links (требует дополнительной настройки)

### 5. Обновлен next.config.ts
- Добавлены headers для assetlinks.json

---

## 🚀 Шаги для развертывания

### Шаг 1: Задеплойте обновленный код на Vercel

```bash
cd /Users/d.chistoforov/moodflow
git add .
git commit -m "Add mobile OAuth support with polling mechanism"
git push
```

После push Vercel автоматически задеплоит обновления.

### Шаг 2: Настройте redirect URLs в Supabase

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Authentication → URL Configuration**
4. В **Redirect URLs** добавьте:
   ```
   https://moodflow-six.vercel.app/callback
   https://moodflow-six.vercel.app/api/auth/callback
   http://localhost:3000/callback
   http://localhost:3000/api/auth/callback
   ```

### Шаг 3: Настройте Google OAuth Consent Screen

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Выберите ваш проект
3. Перейдите в **APIs & Services → OAuth consent screen**
4. В **Authorized domains** добавьте:
   ```
   vercel.app
   ```

### Шаг 4: Обновите Authorized redirect URIs

1. В Google Cloud Console перейдите в **APIs & Services → Credentials**
2. Выберите ваш OAuth 2.0 Client ID
3. В **Authorized redirect URIs** добавьте:
   ```
   https://moodflow-six.vercel.app/callback
   https://moodflow-six.vercel.app/api/auth/callback
   https://YOUR_SUPABASE_PROJECT.supabase.co/auth/v1/callback
   ```

---

## 📱 Шаги для настройки APK (опционально для App Links)

### Получение SHA-256 fingerprint

Если вы создали APK через Google Play Studio или используете TWA:

1. Найдите ваше приложение в Google Play Console
2. Перейдите в **Release → Setup → App integrity**
3. Скопируйте **SHA-256 certificate fingerprint**

### Обновление assetlinks.json

1. Откройте `/Users/d.chistoforov/moodflow/public/.well-known/assetlinks.json`
2. Замените значения:
   ```json
   {
     "package_name": "ваш.package.name",
     "sha256_cert_fingerprints": ["ВАШ_SHA256_FINGERPRINT"]
   }
   ```
3. Задеплойте изменения на Vercel

### Проверка assetlinks.json

После деплоя проверьте доступность файла:
```
https://moodflow-six.vercel.app/.well-known/assetlinks.json
```

### Настройка в Google Play Console

1. Откройте ваше приложение в Google Play Console
2. Перейдите в **Advanced settings → Deep links**
3. Добавьте:
   - Host: `moodflow-six.vercel.app`
   - Path: `/callback`

---

## 🧪 Тестирование

### Тест 1: Веб-версия
1. Откройте https://moodflow-six.vercel.app
2. Нажмите "Войти через Google"
3. После входа вас должно перенаправить на календарь

### Тест 2: Мобильная версия (браузер)
1. Откройте https://moodflow-six.vercel.app на мобильном устройстве
2. Нажмите "Войти через Google"
3. Завершите вход в браузере
4. Закройте браузер вручную
5. Вернитесь на вкладку приложения
6. Приложение должно автоматически перенаправить на календарь (в течение 2 секунд)

### Тест 3: APK версия
1. Установите APK на устройство
2. Откройте приложение
3. Нажмите "Войти через Google"
4. Откроется браузер, завершите вход
5. Увидите сообщение "Закройте окно и вернитесь в приложение"
6. Закройте браузер или нажмите кнопку "Закрыть окно"
7. Вернитесь в приложение (через переключатель приложений)
8. Приложение должно автоматически определить авторизацию и перенаправить на календарь

---

## ❓ Troubleshooting

### Приложение все еще висит на "Вход..."

**Причины:**
1. Браузер не сохранил cookies от Supabase
2. Приложение не может получить доступ к session storage

**Решение:**
- Убедитесь, что в APK включены cookies и storage
- Проверьте в Google Play Studio settings что **Enable Storage** включен
- Попробуйте перезапустить приложение после входа

### OAuth редирект показывает ошибку

**Проверьте:**
1. Redirect URLs в Supabase совпадают с фактическими URLs
2. Authorized redirect URIs в Google Cloud Console включают все необходимые URLs
3. Domain verification выполнена для вашего домена в Google Cloud Console

### Polling не работает

**Проверьте в браузере (DevTools):**
1. Откройте DevTools (F12)
2. Перейдите в Network tab
3. Нажмите "Войти через Google"
4. После входа должны быть видны запросы каждые 2 секунды
5. Если запросов нет - проблема с JavaScript execution в APK

---

## 🔒 Безопасность

Текущая реализация:
- ✅ Использует стандартный OAuth flow с authorization code
- ✅ Cookies с HttpOnly флагом
- ✅ CSRF защита через Supabase
- ✅ Polling только на стороне клиента (нет дополнительных серверных запросов)
- ⚠️ Polling может быть заблокирован в некоторых браузерах с агрессивными настройками приватности

Для production рекомендуется:
- Настроить Android App Links полностью (требует verified domain)
- Рассмотреть использование Custom Tabs вместо браузера
- Добавить rate limiting на API endpoints

---

## 📝 Альтернативные решения

Если polling не работает стабильно, рассмотрите:

### 1. Deep Linking с Custom URL Scheme
```
moodflow://callback
```
Требует:
- Регистрация custom URL scheme в AndroidManifest.xml
- Изменение OAuth redirect URL
- Не работает с обычными веб-приложениями

### 2. Custom Chrome Tabs
- Более нативный experience
- Лучше сохраняет session
- Требует нативной интеграции (не работает с TWA)

### 3. In-App Browser
- Полный контроль над процессом
- Может нарушать политику некоторых OAuth провайдеров
- Требует дополнительной разработки

---

## 📧 Поддержка

Если у вас остались вопросы:
1. Проверьте логи в Vercel Dashboard
2. Проверьте логи в Supabase Dashboard → Logs
3. Используйте DevTools для отладки в браузере
4. Проверьте console.log в приложении

---

## ✅ Checklist

- [ ] Код задеплоен на Vercel
- [ ] Redirect URLs настроены в Supabase
- [ ] Authorized redirect URIs настроены в Google Cloud Console
- [ ] Authorized domains добавлены в OAuth consent screen
- [ ] assetlinks.json доступен по URL (опционально)
- [ ] Протестировано в веб-браузере
- [ ] Протестировано в мобильном браузере
- [ ] Протестировано в APK приложении
- [ ] Пользователи проинформированы о процессе входа

---

## 🎉 Готово!

Теперь ваше приложение поддерживает OAuth вход как в веб-версии, так и в APK!

