# 🔧 Окончательное исправление ошибки RLS при отправке аудио

## 🐛 Проблема

После применения миграций 008 и 009 ошибка все еще сохраняется:
```
new row violates row-level security policy
```

**Причина:** Функция `user_owns_audio_entry` использует `auth.uid()`, который может возвращать NULL в серверном контексте API routes, даже когда пользователь аутентифицирован.

## ✅ Решение

### Вариант 1: Миграция 010 (Рекомендуется)

Использует JWT claims напрямую с fallback на `auth.uid()`:

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Создайте новый query и скопируйте весь код из файла:
   ```
   supabase/migrations/010_fix_audio_entries_rls_jwt.sql
   ```
5. Нажмите **Run** для выполнения

### Вариант 2: Миграция 011 (Альтернатива)

Использует упрощенную функцию `get_current_user_id()`:

1. В том же **SQL Editor** создайте новый query
2. Скопируйте весь код из файла:
   ```
   supabase/migrations/011_fix_audio_entries_rls_simple.sql
   ```
3. Нажмите **Run** для выполнения

## 🔍 Что исправлено:

### Миграция 010:
- Функция `user_owns_audio_entry` теперь сначала пытается получить user_id из JWT claims
- Если JWT claims недоступны, использует `auth.uid()` как fallback
- Это решает проблему, когда `auth.uid()` возвращает NULL в серверном контексте

### Миграция 011:
- Создает новую функцию `get_current_user_id()` которая возвращает user_id из JWT или auth.uid()
- Политики используют эту функцию напрямую
- Более простой и понятный подход

## ⚠️ Важно:

1. **Примените только ОДНУ из миграций** (010 или 011), не обе сразу
2. Рекомендуется начать с миграции 010
3. Если миграция 010 не поможет, попробуйте миграцию 011
4. После применения миграции попробуйте отправить аудио снова

## 🔍 Диагностика:

Если ошибка все еще сохраняется:

1. Проверьте, что миграция применена успешно:
   ```sql
   SELECT * FROM pg_policies WHERE tablename = 'audio_entries';
   ```

2. Проверьте, что функция создана:
   ```sql
   SELECT proname FROM pg_proc WHERE proname LIKE '%audio_entry%';
   ```

3. Проверьте логи в Supabase Dashboard → Logs → Postgres Logs

4. Убедитесь, что пользователь аутентифицирован (есть активная сессия)

5. Проверьте, что `user_id` в запросе соответствует `auth.users.id` пользователя

## 💡 Технические детали:

- `SECURITY DEFINER` позволяет функции обходить RLS и правильно проверять контекст аутентификации
- JWT claims доступны через `current_setting('request.jwt.claims', true)::json->>'sub'`
- Это работает даже когда `auth.uid()` возвращает NULL в серверном контексте

