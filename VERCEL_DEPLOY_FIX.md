# 🚀 Исправление ошибки деплоя на Vercel

## ❌ Проблема

```
Error: > Couldn't find any `pages` or `app` directory.
Please create one under the project root
```

## 🔍 Причина

Vercel пытается деплоить из **корневой директории репозитория**, но ваш репозиторий находится в папке `moodflow/`. Vercel не понимает, где искать Next.js приложение.

## ✅ Решение: Настройка Root Directory в Vercel

### Способ 1: Через Vercel Dashboard (Самый простой ⭐)

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект **MoodFlow**
3. Перейдите в **Settings** (⚙️ справа сверху)
4. Прокрутите вниз до секции **Root Directory**
5. Нажмите **Edit**
6. В поле введите: `.` (точка, так как сам проект уже в правильной папке)
7. Нажмите **Save**
8. Перейдите на вкладку **Deployments**
9. Нажмите **Redeploy** на последнем деплое

### Способ 2: Через Vercel CLI

```bash
# Установите Vercel CLI глобально (если ещё не установлен)
npm i -g vercel

# Перейдите в директорию проекта
cd /Users/d.chistoforov/Desktop/MoodFlow/moodflow

# Задеплойте проект
vercel --prod
```

При первом запуске CLI спросит:
- **Set up and deploy?** → Yes
- **Which scope?** → Выберите ваш аккаунт
- **Link to existing project?** → Yes (если проект уже создан)
- **What's the name of your existing project?** → moodflow

### Способ 3: Переподключить проект

Если ничего не помогает, можно заново подключить репозиторий:

1. В Vercel Dashboard → **Add New...** → **Project**
2. Выберите ваш GitHub репозиторий
3. В настройках импорта найдите **Root Directory**
4. Выберите папку где находится Next.js проект (или оставьте `.` если репозиторий уже в правильной папке)
5. Нажмите **Deploy**

## 🎯 Проверка

После деплоя убедитесь:
- ✅ Build успешно завершился
- ✅ Сайт открывается по URL
- ✅ Переменные окружения настроены (SUPABASE_URL, SUPABASE_ANON_KEY и т.д.)

## 📝 Переменные окружения

Не забудьте добавить в Vercel все переменные из `.env.local`:

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
PERPLEXITY_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
```

## 🆘 Если всё ещё не работает

1. Проверьте, что в корне проекта есть:
   - `next.config.ts` ✅
   - `package.json` ✅
   - Папка `src/app/` ✅

2. Проверьте логи деплоя в Vercel Dashboard

3. Попробуйте локально:
   ```bash
   npm run build
   npm run start
   ```

## 📚 Полезные ссылки

- [Vercel Root Directory Configuration](https://vercel.com/docs/projects/project-configuration#root-directory)
- [Vercel CLI Documentation](https://vercel.com/docs/cli)
- [Next.js Deployment](https://nextjs.org/docs/deployment)



