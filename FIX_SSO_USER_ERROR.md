# 🔧 Исправление ошибки после SSO логина

## 🐛 Проблема

После успешной авторизации через Google SSO на странице календаря возникает ошибка:
```
Application error: a server-side exception has occurred
```

**Причина:** Пользователь создается в `auth.users` (Supabase Auth), но не создается автоматически в таблице `public.users`. Когда API пытается загрузить записи календаря, он не находит пользователя и возвращает ошибку.

## ✅ Решение

### Шаг 1: Применить миграцию (обязательно!)

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (в левом меню)
4. Создайте новый query и вставьте код из файла:
   ```
   supabase/migrations/002_auto_create_user.sql
   ```
5. Нажмите **Run** для выполнения

Это создаст trigger, который автоматически будет создавать пользователя в `public.users` при регистрации через SSO.

### Шаг 2: Создать пользователя для уже залогиненного через SSO

Если вы уже залогинились через Google SSO, нужно создать запись вручную:

1. В том же **SQL Editor** выполните следующий запрос:

```sql
-- Этот запрос создаст записи для всех пользователей из auth.users,
-- которых еще нет в public.users
INSERT INTO public.users (sso_uid, email, full_name, avatar_url, subscription_tier, subscription_status)
SELECT 
  id::text as sso_uid,
  email,
  COALESCE(raw_user_meta_data->>'full_name', raw_user_meta_data->>'name') as full_name,
  raw_user_meta_data->>'avatar_url' as avatar_url,
  'free' as subscription_tier,
  'active' as subscription_status
FROM auth.users au
WHERE NOT EXISTS (
  SELECT 1 FROM public.users pu WHERE pu.sso_uid = au.id::text
);
```

2. Нажмите **Run**

### Шаг 3: Проверка

1. Обновите страницу в браузере
2. Страница календаря должна загрузиться без ошибок
3. Вы должны увидеть пустой календарь (так как еще нет записей)

## 🔍 Проверка работы trigger

Чтобы убедиться, что trigger работает для новых пользователей:

```sql
-- Проверить, что trigger существует
SELECT * FROM pg_trigger WHERE tgname = 'on_auth_user_created';

-- Проверить, что функция существует
SELECT proname FROM pg_proc WHERE proname = 'handle_new_user';
```

## 🆘 Если проблема сохраняется

1. **Проверьте логи в Supabase:**
   - Перейдите в **Logs** → **Edge Functions**
   - Проверьте ошибки

2. **Проверьте, что пользователь создан:**
   ```sql
   SELECT * FROM public.users;
   SELECT * FROM auth.users;
   ```

3. **Проверьте переменные окружения:**
   - Убедитесь, что `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY` правильные
   - Перезапустите dev server после изменений

## 📝 Что было исправлено

1. Создан trigger `on_auth_user_created`, который автоматически создает пользователя в `public.users`
2. Trigger срабатывает при INSERT или UPDATE в `auth.users`
3. Если пользователь уже существует (по `sso_uid`), данные обновляются
4. Для уже зарегистрированных пользователей создается запись через SQL

---

**После выполнения всех шагов проблема должна быть решена! 🎉**






